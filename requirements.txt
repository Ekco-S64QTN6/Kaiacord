# Lean Kaiacord Requirements (Post-Vision/Image Purge)
# ===================================================

# Core Infrastructure
discord.py[voice]==2.7.1
# Voice: discord.py needs PyNaCl to encrypt the RTP stream and libopus from
# the system (libopus.so) to encode it.
#
# `davey` and discord.py >= 2.7 are BOTH required, not optional. Discord
# enforced its DAVE end-to-end-encryption protocol for all non-stage voice on
# 2 March 2026; a client without it is refused at the handshake with close
# code 4017 and can never join a voice channel. discord.py 2.6.4 has no DAVE
# support at all, so voice fails with five retries and "Not connected to
# voice" no matter what the bot does. The [voice] extra pulls both in.
PyNaCl>=1.5.0
davey>=0.1.6
# The !music engine drives Strudel in a local Chromium. Install the browser
# once with: python -m playwright install chromium
# (or run tools/maintenance/fetch_music_assets.py, which does that too).
playwright==1.62.0
ollama==0.6.1
aiohttp==3.13.3
python-dotenv==1.2.1
PyYAML==6.0.3
psutil==7.2.1
rich==14.2.0
google-genai>=1.0.0
python-box==7.2.0
Pillow>=10.2.0


# LlamaIndex RAG (Ollama-based)
llama-index-core==0.14.12
llama-index-llms-ollama==0.9.1
llama-index-embeddings-ollama==0.8.6
llama-index-readers-file==0.5.6
llama-index-workflows==2.12.2

# Search & Retrieval
rank-bm25==0.2.2
nltk==3.9.2
numpy==2.4.1
pandas==2.3.3

# Document Processing
pypdf==6.6.0
docx2txt==0.9
python-docx==1.2.0
beautifulsoup4==4.14.3
# !youtube — fetches video transcripts (no API key required)
youtube-transcript-api==1.2.4
markdownify==0.11.6

# Social Media
atproto>=0.0.54
twikit>=2.0.0
browser_cookie3>=0.19.1

# System & Monitoring
fastapi==0.128.0
uvicorn==0.40.0
uvloop==0.22.1
watchfiles==1.1.1
